# Host port exposed by docker-compose.
KIROPROXY_PORT=8080

# Upstream KiroProxy git ref used when building locally.
KIROPROXY_REF=main

# Optional: uncomment if your imported credentials need an explicit profile ARN.
# KIRO_PROFILE_ARN=arn:aws:...
